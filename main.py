from delivery_ui import Tool

USER_INPUT = True
INPUT_VALUES = (1, 1, 0, [[0, 0], [1, 3, 0.2]]) # topic_pt, predicting, retrain, transformations

Tool(user_input=USER_INPUT, input_values=INPUT_VALUES)